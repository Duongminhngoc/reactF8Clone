import { createSelector } from "@reduxjs/toolkit";

export const coursesProSelector = (state) => state.coursesPro;

export const coursesFreeSelector = (state) => state.coursesFree;

export const postsSelector = (state) => state.postsOutstanding;
